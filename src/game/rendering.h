#include <stdlib.h>

// Set to the amount of level blocks we have.
#define COUNT_LEVELBLOCK (5)
uint64 randInt(uint64 seed);

void renderLevel(uint64 seed) {
	//uint64 chosenBlock = randInt(levelSeed) & (COUNT_LEVELBLOCK + 1 - 0) + 0;
}